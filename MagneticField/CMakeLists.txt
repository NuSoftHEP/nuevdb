
simple_plugin( MagneticField service
               ${MF_MESSAGELOGGER}
               ${MF_UTILITIES}
               ${CLHEP}
               ${ROOT_BASIC_LIB_LIST}
               ${ROOT_GEOM}
               BASENAME_ONLY )

install_headers()
install_fhicl()
install_source()
