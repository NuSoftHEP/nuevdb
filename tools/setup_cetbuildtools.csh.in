# This script must be sourced
# This script allows users NOT using mrb or cetbuildtools to find checkClassVersion


if ( $?UPS_DIR == 0 ) then
   echo "ERROR: please setup ups"
   exit 1
endif

source `${UPS_DIR}/bin/ups setup cetbuildtools @CETBUILDTOOLS_VERSION@`
